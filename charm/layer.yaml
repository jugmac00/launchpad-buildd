repo: lp:launchpad-buildd
includes:
    - layer:basic
    - layer:apt
options:
    apt:
        packages:
            - bzr-builder
            - git-build-recipe
            - launchpad-buildd
            - quilt
ignore:
    - dist
    - tmp
